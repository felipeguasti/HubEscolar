// src/modules/broadcast/routes/broadcasts.js
const express = require('express');
const router = express.Router();

// Defina as rotas para o módulo Broadcast aqui
// Por exemplo:
// router.get('/', (req, res) => res.send('Broadcasts Route'));
// router.post('/new', (req, res) => res.send('Create new broadcast'));

module.exports = router;
